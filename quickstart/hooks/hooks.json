{
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Display the Datadog PoC Quickstart menu to the user. Present the numbered use-case list grouped by category (Agent Setup, APM Instrumentation, Frontend Monitoring, Log Management, Cloud Integrations, Troubleshooting). Include a note: '(Type \"skip\" to dismiss this menu.)' at the bottom. Use the showing-menu command format. Keep it compact — one line per item, no action instructions shown."
        }
      ]
    }
  ],
  "TaskCompleted": [
    {
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Before marking this task complete, ask the user: 'Would you like me to document the steps taken for this task to a PoC notes file?' If the user agrees, write a concise entry to the PoC notes markdown file (default: ./datadog-poc-notes.md, or the path configured in .claude/quickstart.local.md). The entry should be appended with a timestamp header and include: (1) Tech stack — languages, versions, frameworks involved, (2) Steps taken — what was configured or installed, (3) Problem — what issue was being solved, if any, (4) Solution — what worked or what was recommended, (5) Limitations — any known caveats or incomplete items. Keep each section to 1-3 bullet points. If the task was not Datadog-related, skip documentation gracefully."
        }
      ]
    }
  ],
  "SessionEnd": [
    {
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Ask the user: 'Would you like me to review the PoC notes before ending this session?' If the user agrees, read the PoC notes markdown file (default: ./datadog-poc-notes.md, or the path configured in .claude/quickstart.local.md). Review for completeness and conciseness — suggest any missing information (e.g., versions not noted, limitations not documented) while keeping the file concise. Do not add verbose explanations. If the file does not exist or the session had no Datadog-related work, skip gracefully."
        }
      ]
    }
  ]
}
