apiVersion: v1
kind: Service
metadata:
  name: springboot-ilb
  annotations:
    networking.gke.io/load-balancer-type: "Internal"
spec:
  type: LoadBalancer
  selector:
    app: springboot3dot5dot9-sandbox
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP